// Plain enum-code -> human-label display map (distinct from the
// value/label array-of-pairs pattern already covered by enum-label-pair.ts).
export const receiptStatusLabels: Record<string, string> = {
  PENDING_ISSUE: "Pending issue",
  RETURNED: "Returned",
};
