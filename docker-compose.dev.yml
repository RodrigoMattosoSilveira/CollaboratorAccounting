services:
  backend:
    volumes:
      - ./backend:/app/backend
    command: go run ./cmd/server

  frontend:
    volumes:
      - ./frontend:/app
    command: npm run dev -- --host
    ports:
      - "5173:5173"
